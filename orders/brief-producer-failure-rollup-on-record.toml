[order]
description = "Roll up producer-failure records after brief-quality failures are recorded."
scope = "city"
formula = "brief-producer-failure-rollup"
trigger = "event"
on = "brief.quality_failure_recorded"
pool = "mathcity.brief-operator"
timeout = "180s"
idempotent = true
