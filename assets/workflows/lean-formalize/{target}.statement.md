# Stage: statement

This step expands the `lean-formalize-statement` formula. Follow that expansion's own
step descriptions.

Do not re-derive the previous stage's output — read it. Do not skip ahead: each
stage's gate exists because the next stage is meaningless without it.

If this stage cannot complete, stop and report WHY rather than proceeding with a
partial result. A partial baseline, a partial claim inventory, or a partial
representation design silently corrupts every stage after it.
