# Verify the formalization and report

This step expands `lean-formalize-verify`. Follow that expansion's step
descriptions.

## Terminal report

- Claims in scope, claims formalized, claims recorded as gaps.
- Build command, exit code, axiom summary.
- The three-count reconciliation: sorries, gap entries, beads.
- Fidelity verdict per claim.

Report the numbers you measured, and say where each came from. A count you did not
run is not a count.
