# Structural sections a brief body must carry, in ONE place.
#
# #169: `briefs_create` never parsed the body, so a brief missing Gate Evidence
# was accepted, written, and reported as success -- with the refusal deferred to
# `brief-check.sh pile-entry` at shuffle time, against an author already told the
# operation succeeded. That is the CT13.4 shape, and #96 measured the cost: the
# pile drained 5 -> 0 entirely by auto-reject.
#
# WHY A DATA FILE. The issue's candidate 2 says reuse the existing rule rather
# than write a second checker, because two independently written structural
# checkers drift -- that is what #35 was about. But the rule was triplicated
# INSIDE brief-check.sh (lines 293, 300, 331), so there was no single existing
# rule to reuse. There is now.
#
# `match` is the exact regex brief-check.sh uses, character for character. A test
# asserts brief-check.sh still contains it, so the day the two disagree is the
# day that test fails rather than the day a brief is silently destroyed.
#
# brief-check.sh is NOT yet pointed at this file. It gates the live drain and
# changing it is a wider blast radius than this issue; that is a follow-up.

[[section]]
name = "Gate Evidence"
match = '^##*[[:space:]]+([^A-Za-z]*[[:space:]])?Gate Evidence\b|^Gate Evidence\b'
reason = "the pipeline rejects a pile entry without it, at shuffle time, after creation reported success"

# DECISION PROFILE ONLY.
#
# `brief-check.sh check_action_block` is called from `check_decision_profile`
# and nowhere else, so this is scoped rather than global: a `lost_bead_filter`
# or `producer_repair` brief correctly carries no action_block, and requiring
# one of them would refuse briefs that are fine.
#
# Why it is here at all: `## Gate Evidence` was the ONLY create-time rule, so a
# decision brief missing its action_block passed creation, was reported
# `applied: true`, and was then bounced at shuffle time. Measured on the kolchin
# testrig 2026-09-08 -- brief `mt-yftq`, created through `briefs_create`, landed
# in `.pile/.rejected/` with `reason: "decision brief missing action_block"`.
# That is the CT13.4 shape #96 quantified as "pile drained 5 -> 0 entirely by
# auto-reject".
#
# `match` is brief-check.sh:188's regex character for character; the reader
# translates `[[:space:]]` to `\s`.
[[section]]
name = "action_block"
profile = "decision"
match = '^action_block:[[:space:]]*$'
reason = "the decision profile rejects a pile entry without it, at shuffle time, after creation reported success"

# THE FULL FORM, §1-§7 (#219). ADR 0001 (2026-08-24): "every brief is full-form
# §1-§7; the compact form is retired."
#
# WHY THESE MATCH ONLY THE MARKER, not the title. The composer emits
# "## §4 — Alternatives named" in one path and "## §4 — Options" in another
# (decisions.py, when options are authored), and briefs.py's own parser accepts
# `§N`, `section N` and `sec. N` at any heading depth (_EXPLICIT_SECTION). A
# rule keyed on the title would refuse a brief that is correctly formed and
# merely worded differently -- so these key on the SECTION MARKER alone.
#
# WHY create_only, and what closes it. Adding these to brief-check.sh at the
# same time would switch on retroactive drain enforcement: 3 of 12 brief files
# on the kolchin testrig are compact form, and section-discipline.toml records
# 59 of 178 on the wider corpus. Every one would be auto-rejected -- the exact
# #96 harm this file exists to prevent. A gate rejection is also not a revise
# verdict, so revise-return (#209) does not catch them either.
#
# So: NEW briefs are full-form from now; EXISTING ones are left alone. The flag
# comes off when the pile is drained of compact-form entries, or when a gate
# rejection can be routed to revise-return instead of the reject pile. That is
# the second half of #219 and is a deliberate step, not a cleanup.

[[section]]
name = "§1 — What is being decided"
profile = "decision"
create_only = true
migration_note = "#219: drain-side enforcement would auto-reject the compact-form briefs already in the pile; flag comes off once they are drained or routable to revise-return."
match = '^#{1,6}[[:space:]]*(§|[Ss]ection[[:space:]]+|[Ss]ec\.[[:space:]]*)1\b'
reason = "ADR 0001 retires the compact form; a decision brief that omits it cannot be read by the §-scan the structure exists to support"

[[section]]
name = "§2 — Recommended answer"
profile = "decision"
create_only = true
migration_note = "#219: drain-side enforcement would auto-reject the compact-form briefs already in the pile; flag comes off once they are drained or routable to revise-return."
match = '^#{1,6}[[:space:]]*(§|[Ss]ection[[:space:]]+|[Ss]ec\.[[:space:]]*)2\b'
reason = "ADR 0001 retires the compact form; a decision brief that omits it cannot be read by the §-scan the structure exists to support"

[[section]]
name = "§3 — Assumptions surfaced"
profile = "decision"
create_only = true
migration_note = "#219: drain-side enforcement would auto-reject the compact-form briefs already in the pile; flag comes off once they are drained or routable to revise-return."
match = '^#{1,6}[[:space:]]*(§|[Ss]ection[[:space:]]+|[Ss]ec\.[[:space:]]*)3\b'
reason = "ADR 0001 retires the compact form; a decision brief that omits it cannot be read by the §-scan the structure exists to support"

[[section]]
name = "§4 — Alternatives named / Options"
profile = "decision"
create_only = true
migration_note = "#219: drain-side enforcement would auto-reject the compact-form briefs already in the pile; flag comes off once they are drained or routable to revise-return."
match = '^#{1,6}[[:space:]]*(§|[Ss]ection[[:space:]]+|[Ss]ec\.[[:space:]]*)4\b'
reason = "ADR 0001 retires the compact form; a decision brief that omits it cannot be read by the §-scan the structure exists to support"

[[section]]
name = "§5 — Risks foregrounded"
profile = "decision"
create_only = true
migration_note = "#219: drain-side enforcement would auto-reject the compact-form briefs already in the pile; flag comes off once they are drained or routable to revise-return."
match = '^#{1,6}[[:space:]]*(§|[Ss]ection[[:space:]]+|[Ss]ec\.[[:space:]]*)5\b'
reason = "ADR 0001 retires the compact form; a decision brief that omits it cannot be read by the §-scan the structure exists to support"

[[section]]
name = "§6 — Supporting evidence"
profile = "decision"
create_only = true
migration_note = "#219: drain-side enforcement would auto-reject the compact-form briefs already in the pile; flag comes off once they are drained or routable to revise-return."
match = '^#{1,6}[[:space:]]*(§|[Ss]ection[[:space:]]+|[Ss]ec\.[[:space:]]*)6\b'
reason = "ADR 0001 retires the compact form; a decision brief that omits it cannot be read by the §-scan the structure exists to support"

[[section]]
name = "§7 — Plan membership, blocking, and required gates"
profile = "decision"
create_only = true
migration_note = "#219: drain-side enforcement would auto-reject the compact-form briefs already in the pile; flag comes off once they are drained or routable to revise-return."
match = '^#{1,6}[[:space:]]*(§|[Ss]ection[[:space:]]+|[Ss]ec\.[[:space:]]*)7\b'
reason = "ADR 0001 retires the compact form; a decision brief that omits it cannot be read by the §-scan the structure exists to support"
