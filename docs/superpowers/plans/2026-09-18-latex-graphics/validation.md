# Validation: Sage graphics and LaTeX example skills

Scope: generate-graphics, add-figure, write-example; using-latexpowers and
explain-experiment routing; shared writer roster and README skill indexes.
No mathematical research data are included in this package fixture.

## Fixed-point evidence

Six independent per-file critics reviewed the five SKILL.md files and the
graphics provenance reference. Initial reports found a reciprocal dispatch
loop between add-figure/write-example and a Sage cache guidance omission.
The revisor fixed both. Measured byte changes: generate-graphics 4051→4028,
write-example 2689→2673, add-figure 3508→3474. Each delta was substantive.
The other three files were unchanged (3897, 2031, 2623 bytes). All changed
paths were re-reviewed and received APPROVING with zero findings; unchanged
paths retained their approvals. Tuple-level floor passed. The mutual-approval
revisor returned NO. Result: converged after one accepted revision, N=2.
No padding, factoring, or quality-floor bypass was used.

## Executed checks

- Three skill validators passed.
- Gitleaks found no secrets in the scoped overlay.
- Sage smoke test: exact C4 degrees, adjacency spectrum, characteristic
  polynomial, trace identities; fresh-directory rerun agreement; source/output
  hashes; deliberate corruption detection; refusal to overwrite output.
- Render inspection: four labels/four edges, no clipping; vector PDF and PNG.
- Both local skill tables: 23 entries; root table: 177, with unique names.
- Relative resource links and three agent-skills symlinks resolve.
- Isolated city: gc import install/check passed; gc skill list exposed all
  three mathcity-latex names. Local-path imports have no remote lock entries.
  This tests catalog availability, not a live provider materialization: no
  city controller or agent was started. Missing core/bd import warnings in
  the intentionally minimal fixture are not concealed as a full city pass.

The behavioral scenarios are review prompts; they are not reported as an
automated agent scenario suite. No unexecuted command is marked successful.

## Installed checkout verification

The three validators and Sage smoke fixture passed again against the installed
checkout. The isolated city now imports the actual subdomains/latex directory;
import install/check passed and the three names appeared in gc skill list.
The 2026-09-18 installed-manifest.json fingerprints the delivered source.

Beads synchronization could not run: Mathcity refused remote-backed schema
migration v64 to v66; agent-skills reported local v55 with an already migrated
remote. No migration, bootstrap, database replacement, or issue mutation was
attempted. These infrastructure failures do not change the skill test results.
