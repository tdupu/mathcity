# Issue triage — the remaining 89 open issues (`tdupu/mathcity`)

**Date:** 2026-08-28 · **Mode:** READ-AND-REPORT ONLY (nothing closed, commented, labelled, or edited)
**Source of truth for code:** `origin/main` = `51ddb27`. The local working tree is 22 commits behind and was never used as evidence.
**Method:** six parallel triage agents, ~15 issues each, every row carrying a probe that could have returned the other answer. Cross-cutting analysis (counts, bead axis, reference graph) run separately and used to *check* the agents, not to replace them.

## Baseline verification

| Check | Claimed | Measured | Verdict |
|---|---|---|---|
| Open issues | 107 | **107** (`--limit 500`) | confirmed |
| Closed issues | 113 | **113** (`--limit 1000`) | confirmed |
| In scope (107 − 18 already-done) | ~89 | **89** | confirmed |
| Bead store is `mc-` prefixed, not an HQ fallback | — | **1166 rows, 1166 `mc-`** | confirmed |

## Summary counts by disposition

| Disposition | Count | Share |
|---|---:|---:|
| **ACTIONABLE** | **53** | 60% |
| **ALREADY-FIXED** | **22** | 25% |
| **STALE-PREMISE** | **7** | 8% |
| **DECISION** | **6** | 7% |
| **DUPLICATE** | **1** | 1% |
| **UNKNOWN** | **0** | 0% |
| **Total** | **89** | 100% |

**29 of 89 (33%) are closeable today** — 22 ALREADY-FIXED + 7 STALE-PREMISE. But see the warning below: **two of the seven must be re-scoped, not closed** (#80, #218).

Size distribution of the 53 ACTIONABLE: **S 21 · S–M 2 · M 24 · M–L 1 · L 5**.

---

## Full table

Evidence is `[measured]` unless stated otherwise. The "Bead(s)" column records the duplicate axis — see the bead-hygiene section for why a *closed* bead almost never means "fixed" in this repo.

### Batch A — #59–#84

| # | Title (short) | Disposition | Evidence | Bead(s) | Size |
|---|---|---|---|---|---|
| 59 | Umbrella: mechanical skills → typed MCP tools | ACTIONABLE | Roster is now **48** tools (`mcp_server.py` `TOOLS`) vs 15 in the body / 26 in the comment. The issue-filing ask is satisfied (`create_github_issue`, `create_issue_bead`, `standardize_github_issue`, `create_defect_bead`, `bead_comment`). Umbrella survives because #61/#62/#63/#65 have residuals | `mc-q8tp` open; no commission bead | L |
| 60 | MCP conversion D1–D5 | **ALREADY-FIXED** | All five ruled 2026-08-19 and codified as Pillar 7 / P7.1–P7.3, `subdomains/dev/POLICY.md:596-623`. Residual: `POLICY-skills.md` has 0 `mctl` mentions | `mc-7sl0` open / `mc-j4js` closed | S residual |
| 61 | Verify each probe before wrapping it | ACTIONABLE | No per-skill probe inventory. `SKILL-IMPACT-REGISTER.md` is a scope checklist with no verdicts. MBRF021 mitigated not fixed (`mcp_server.py:208 UNTRUSTED_ARTIFACT_CODES`); MBRF004 population 120 → 71 | `mc-87zg` open / `mc-t6kj` closed | M |
| 62 | Convert the `check-*` family | ACTIONABLE | mctl mentions: `check-briefs` 32 (the #41 pilot); `check-work` **0**, `check-molecules` **0**, `city-status` **0**. Target tools already exist (`city_health`, `molecules_list/show`, `work_ready/status`) — scope is skill-side rewiring, not new tools | `mc-uxsh` open / `mc-ts24` closed | M |
| 63 | Convert the work skills | ACTIONABLE | `work` 32, `priority-work` 23, `immediate-work` 13 converted; `simple-work` **0**, `push-the-fleet` **0**. 2 of 5 remain | `mc-yflm` open / `mc-7y01` closed | S |
| 64 | prime-clerk / mayor-math-* bash blocks | **STALE-PREMISE** | Body claims "all of these mention `mctl`/`mcp` exactly **zero** times" — false on main: `prime-clerk` **40**, `mayor-math-prime` **25**, `mayor-math` **17**, `mayor-math-handoff` **17**. Only `mayor-math-restart` (0, justified in `1bea1a7` — reads no brief/work state) remains | `mc-g9w0` open / `mc-4p3z` closed | S (1 file) |
| 65 | Brief state location: one Python resolver | ACTIONABLE | 2 of 3 named instances closed; the resolver landed (`redundant_state.py artifact_layout()`, docstring: "the single resolver"). Still true: `brief-stack-index.py` has `--brief-root required=True` on 3 subcommands and never reads `paths.toml` | **no bead** | S |
| 67 | Every brief must reach the front end | ACTIONABLE | 4 of 5 slices landed, each with a test (HQ store, verdict adapter, gate-token regex, options sources). Residual = slice 5 only: MBRF004 population 71, no recorded decision | **no bead** | M |
| 72 | Lost-bead filter: decided-but-never-started | ACTIONABLE | No back-filter on main (control probe returns non-empty). Alternative A landed — `effects.py:1029-1073` writes `verdict`/`verdict_reason`/`adjudicated_at`/`adjudicated_by`, so *new* adjudications are bead-visible; existing canaries are not. `gs-ab5v` re-measured: open, no verdict field, **41 days** | `mc-8exf` open / `mc-cybo` closed | M |
| 75 | Bead-migration selector | ACTIONABLE | No selector artifact, script, formula or order. ADR 0003 has since landed, so that caveat is stale. #19 is still open, so the block holds | `mc-jx5d` open / `mc-ifwx` closed | M |
| 79 | Canonical dolt backups stale | ACTIONABLE | Independently re-derived (`gh repo view --json pushedAt`), not re-read from the issue: HQ **44d** (was 36 — worse by 8), gascity-packs 33d, hecke 28d, mathcity 18d, differential-valuations 17d. `agent-skills` improved to 2d — the only one that received data. 5 of 6 still stale, every one grown | `mc-ga2l` open / `mc-7ber` closed | L |
| 80 | Reaper times out on **every** fire | **STALE-PREMISE — re-scope, do NOT close** | Own parse of `.gc/events.jsonl`, subject=reaper, 08-13→08-28: **77 failed / 76 completed / 157 fired**. Every failure lands at 300–301s (mechanism confirmed) but completions have median **191s**. 08-27: 10 fired / 10 failed; **08-28: 15 fired, 15 completed** at 142–249s. Marginal and load-shaped, not the deterministic every-fire failure claimed | `mc-bjvz` open / `mc-cfgd` closed | M re-scope |
| 82 | `brief-shuffle.toml` writes `.index.jsonl` outside mctl | ACTIONABLE | Violation persists: `formulas/brief-shuffle.toml:145-152` still `(flock 9; printf … >> {{artifact_root}}/stack/.index.jsonl)`, plus `mv -f` at 56/73/142; `grep -c mctl` = **0**. Secondary claim now stale — registered `role="violation"` since 2026-08-20, so the coverage gap is closed, the defect is not | `mc-2qqo` open / `mc-5r0v` closed | M |
| 83 | `brief-record-decision.toml` writes `decisions/*.toml` | ACTIONABLE | Persists: line 52 still writes the TOML; `grep -c mctl` = **0**; still shells to `brief-stack-index.py` at line 209. Scope-gap claim now stale — the artifact *is* registered `sole_writer="mctl"` | `mc-s9d6` open / `mc-8hqz` closed | M |
| 84 | `brief-review-patrol.toml` frontmatter patch | ACTIONABLE (blocked on #86) | Instruction persists at lines 110-117; `grep -c mctl` = **0**; formula still paused city-wide. Scope note still true: `brief-writers.toml` has exactly four `[[artifact]]` entries and brief frontmatter is not among them | `mc-fbao` open / `mc-esl5` closed | S blocked |

### Batch B — #85–#125

| # | Title (short) | Disposition | Evidence | Bead(s) | Size |
|---|---|---|---|---|---|
| 85 | decisions-to-briefs SKILL never calls mctl | ACTIONABLE | `grep -c mctl` → **0**; direct-write instructions at L62/115/141/153. Sub-claim now stale: it *is* in the register, `role="violation"` | `mc-2t7h` open / `mc-jtez` closed | S |
| 86 | mctl gap: no way to advance `review_gate` | ACTIONABLE | `review_gate` → 3 hits, all prose/reader; `pending-review\|in-review-iter\|review-failed` in `mctl_core/` → **0** (probe proven capable by the 3 hits). `effects.py:1088` writer still hardcoded inside `plan_adjudication`; no `briefs_review` surface. Correction: it now writes **four** keys, not the two the issue states | `mc-x14n` open / `mc-0gc6` closed | M |
| 88 | dashboard: A–E evidence chain + `molecule.state` | ACTIONABLE (narrowed) | **PR #248 vendored specs only** — its 3 files are all docs, zero code. Partial impl landed separately in `f546b02`. Still unbuilt: `molecule.state` 5 values, `evidence.history/.furthest/.last_motion_at`, 3 of 5 link emitters. **≥70% overlap with #115 — merge candidate** | **no bead** | L |
| 90 | `stack/.index.jsonl` has no declared literal | ACTIONABLE | `brief-writers.toml:29-31` has `path` + `sole_writer`, no `literal`/`literals` key before the next `[[artifact]]` (first `literal` is L122, in a different block). Unchanged since filing | `mc-8bt1` open / `mc-o97r` closed | S |
| 94 | no-brainer-disarm targets `/` when GC_RIG_ROOT unset | **ALREADY-FIXED** | `2cf1c93` "gates: an engaged kill switch failed permissive when the rig root did not resolve". `brief-check.sh:797-833` gates the rig token on `NB_RIG_ROOT_RESOLVED` and says so out loud. Live run in a temp city with the var unset → **exit 0, no `/.beads` write** | `mc-ot5k` open (stale — should close) / `mc-a5bv` closed | — |
| 95 | brief stack misreports contents 4 ways | ACTIONABLE | Root cause fixed — single `normalize_stem` (`manifest.py:344`), imported by `documents.py:111`, tested — satisfying the issue's own acceptance #2. But live state is worse: **93** stack `.md` vs **92** index rows; **27** terminal-status briefs still on the pending stack (was 8); 4 slugs in both `stack/` and `.adjudicated-archive/`, incl. the issue's own exemplar. Remaining: apply the normaliser to the *writer* + one reconcile pass | **no bead** | M |
| 96 | pile drained 5→0 by auto-reject | ACTIONABLE | Both named producer causes addressed (`gate_profile: decision` in 3 formulas; Gate Evidence required in the skill). The general defect from the issue's own reframe reproduces: `.pile/.rejected` = **77 dirs in 4 record formats** (`rejection.md` 55, `.json` 12, `rejection-record.md` 7, none 5). Also carries an embedded G5 ruling | **no bead** | M |
| 99 | ready-queue exerts zero pool pressure | **STALE-PREMISE** | Pool demand *is* computed from a `bd ready` query by default: `internal/config/workquery.go:41,111,503`; `build_desired_state.go:4174`. "**Zero** pool pressure" is unsupported. True narrower kernel: the probe counts only `{ready ∧ unassigned ∧ routed_to==target ∧ kind==workflow}` capped at 20, and the 3,301 figure is unfiltered city-wide and does not intersect it. Upstream gascity | **no bead**; `mc-diuc`/`mc-i4fg` are a **different `#99`** (a gascity PR) | — |
| 102 | `brief-manifest-current` passes on a stale index | ACTIONABLE | `brief-check.sh:364-367` `check_manifest()` = `mkdir -p` + `check_jsonl`; `check_jsonl:278` returns 0 on a missing file. Wired into both `brief-record-decision.toml:246` and `brief-watchdog-refill.toml:43`. Fixture A carries an embedded ruling; fixture C (stale) is unambiguous engineering | `mc-v9ev` open / `mc-ahys` closed | S |
| 105 | dispatch discards priority; `unlock_count` unconsumed | **DECISION** | Both halves reproduce upstream: `workquery.go:133` `--sort oldest --limit=20`; `unlock_count` → **14 hits, all in `beads_test.go`**, zero production Go (positive control: 106 `priority` hits). **PR #242 did NOT close this** — it fixed only a `/queue` render crash on a sentinel. Candidates A vs B need a ruling | `mc-pzgu` open / `mc-4rkt` closed | S upstream, after ruling |
| 107 | no-brainer-mode says DRY-RUN, means UNKNOWN | ACTIONABLE | `report_no_brainer_mode` (`brief-check.sh:753-766`) still has exactly two branches; `nb_resolve_mode:745-747` forces dry-run when the root is unresolved. Live sandbox run with the var unset printed "mode: **DRY-RUN** / nothing is executed" and JSON `{"mode":"dry-run"}` with no unresolved flag | `mc-86yu` open / `mc-ru0e` closed | S |
| 115 | dashboard slice 5: evidence log + expected_artifacts | ACTIONABLE (narrowed) | `f546b02` landed the buildable slice: `gc.expected_artifacts.v1`, 3-valued derived `is_complete`, 5-link order, non-positional `_broken_at`. Remaining: emitters for `claimed`/`agent_active`/`commit`, plus `evidence.history`. The comment's "grep → 0 files" is now stale (3 files, 12 hits) | **no bead** | M–L |
| 123 | 143/499 hq molecules have a dangling root | ACTIONABLE (investigation) | Detector landed: `mctl mayor conservation` — `mayor.py:160-196` (`roots_dangling`, `dangling_root_ids`), exit 1 on a dangling root, with "do not repair dangling pointers" encoded at `mayor.py:36-37`. Detection ≠ closure, as the issue itself says. Current count **not re-measured** — see caveats | `mc-ur4o` open / `mc-kyim` closed | M |
| 124 | GOAL 3 conservation ledger | **DECISION** | Rollup of #123 / #77 / #95 + D/E; the actual ask is a ruling — adopt "Class 3 — deleted-with-surviving-references" into the spec, and **gate any cleanup** ("do not tidy the evidence"). Category D still true and grown: `.pile/manifest.jsonl` = **39 rows, 39/39 `status:"ready"`**, `.pile/*.md` = **0 files** | `mc-nn1c` open / `mc-6wo2`, `mc-4zo5` closed | — |
| 125 | briefs dashboard: adjudicate fast | **DECISION** | Substantially built: save-in-place (`panel.py:617`), one-keypress advance with `autofocus` + "N left on this queue" (`app.py:2292`), and an in-code comment citing #125 explaining the POST→302 was **declined deliberately**. Remaining = the A/B/C ruling. Secondary gap: `_advance_after` takes `ids[0]` with no adjudicability check and no skip message | **no bead** | — |

### Batch C — #131–#187

| # | Title (short) | Disposition | Evidence | Bead(s) | Size |
|---|---|---|---|---|---|
| 131 | escalations not queryable | ACTIONABLE | No escalation/mail read in the 48-tool roster. The *indistinguishability* half landed (`mayor.py:585 escalations_queryable`, `MAYOR_ESCALATIONS_NOT_QUERYABLE`: "Boot state cannot answer 'what is escalated'"). The GAP itself untouched; architecture choice still open | `mc-nvyy` open / `mc-aiaa` closed (decision) | M |
| 140 | queue held-back set unreachable | ACTIONABLE (reduced) | PR #242 rewired `/queue` (`app.py:849-921`); `junk_count_note` **does** carry `<a href="/junk">`, so criterion 1 is met. Criterion 2 is not: the note reads "The Junk lane lists more than this number" — a superset the issue explicitly rules out as evidence. The original `held_back_note` is now dead code | **no bead** | S |
| 145 | `bd list --json` omits null fields | ACTIONABLE (upstream) | Live re-probe on `bd 1.2.2`: 3 rows returned **12/13/14 keys**, key sets not identical, **zero** null-valued keys. Claim holds exactly. Serializer is `gastownhall/beads` — not a mathcity change | `mc-lf06` open / `mc-q5s4` closed | S upstream |
| 148 | Q5: one misnamed pile file → whole rig untrusted | **DECISION** | `mcp_server.py:338-352` — a single `_frontmatter_lookup_mismatch` still returns whole-rig `ArtifactTrust(trusted=False)`. Drift: `OPEN-DESIGN-QUESTIONS.md:89` marks Q5 **RESOLVED** while the diagnostic still emits "Q5 must resolve" — and that resolution answers a *different* question than #148 asks | `mc-04ic` open / `mc-uirr` closed | — |
| 162 | stale MCP tool roster (16 vs 23) | **ALREADY-FIXED** | `9d9d591` "stop freezing the MCP tool count; defer to the live roster", merged in `0c85901`. Banner is now `internal_tool_count()`; docs retitled "illustrative, not the roster" and point at `tools/list`; pinned by `test_roster_docs_defer_to_live.py` (5 tests). **Roster re-counted independently: 48**, so the title's numbers went stale a third time — which is the argument *for* the deferral fix that landed | `mc-f63t` open / `mc-yga7` closed | — |
| 163 | `blast_radius` reachable by no caller | ACTIONABLE (reduced) | Half landed: `_handle_blast_radius_registry` (`mcp_server.py:617`, `ddaf1ce`), so "reachable by no caller" is now false. Substantive half stands: `grep -c blast_radius effects.py` → **0**; not on `EffectPlan`, no `_raise_if_unconfirmed`. The new handler's own docstring concedes it | **no bead** | M |
| 168 | `briefs_create` writes no stack-index row | ACTIONABLE (reduced) | `effects.py:381` docstring unchanged ("deliberately NOT written: B2.10"); candidates 1 and 2 both unbuilt. Premise materially weakened: `_handle_briefs_create` now passes `on_apply=_emit_brief_submitted`, so `brief-shuffle-on-submit` fires within seconds — promotion is actively triggered, not merely hoped for | **no bead** | S |
| 171 | orphan-sweep failed 158/163 runs | ACTIONABLE (upstream) | The three most recent runs (all 2026-08-28) **all carry label `exec-failed`**, with no stderr, no exit code, no error text. Both claims confirmed, still failing. **Not mathcity code** — source is the gascity core pack; fix candidate 1 is also upstream | **no bead** | M upstream |
| 178 | path-B order provenance non-atomic | ACTIONABLE | No `#178` reference anywhere on main. `work.py:648 plan_dispatch_event` made the *bead-pair* atomic ("One operation, not two") — candidate A applied **inside** mctl. The reported window is outside it: `gc sling` and `work_dispatch_event` remain two caller-driven calls, and `provenance.py:38` has no reconciliation against runs. A run with no provenance still reads identically to no dispatch | `mc-2ow` open | M |
| 179 | KEYSTONE: issue → dispatchable hygienic brief | **ALREADY-FIXED** | `8976a47` added `skills/github-issues-to-briefs/SKILL.md` — the exact edge: `standardize_github_issue` → `create_issue_bead` → `briefs_create` with **exactly one source (the bead)**, which is the "interesting part" the issue named. Demonstrated, not merely present: **37 beads** titled `github-issue N — …` exist. All four stated blockers now closed. Caveat: shipped as a *skill*, not the formula sibling specified; the typed wrapper is #180 scope | 37 `github-issue N` beads, all closed | — |
| 180 | ONE issue → brief → claimable molecule | ACTIONABLE | Plan D records S48 proving 4 of 5 steps by hand; step 5 was blocked on #99 pool-EMPTY and #181's budget. **#181 has since landed (`f8fa3ac`)**, so it is unblocked. Remaining: re-run the lap on a fresh issue, then wrap as `commission_from_issue` (absent from the 48-tool roster) | none matching | M |
| 181 | `work_dispatch` 120s budget < sling cost | **ALREADY-FIXED** | `f8fa3ac`: `work.py:431 DISPATCH_TIMEOUT_SECONDS = 200`, consumed at `:521`, with `MEASURED_SLING_WORST_SECONDS` recording the 162.7s evidence. Pinned by `test_dispatch_budget.py` (budget ≥ measured worst case) | **no bead** | — |
| 183 | `render_diagnostic` never prints suggested_next_command | ACTIONABLE | `diagnostics.py:78` still enumerates six fields — `suggested_next_command` absent. The issue's own stopgap is still in the tree: `effects.py:2077` copies the remedy into `facts` with a comment naming the line. **Narrowing confirmed:** `to_dict()` emits every typed field, so JSON/MCP consumers already get it — **the defect is CLI-only**, which the issue flagged as unverified | **no bead** | S |
| 185 | typed WRITE path: 3 tools | **ALREADY-FIXED** | Merge `b7d7a50` "Merge Plan A (#185): mctl intake surfaces". All three handlers present (`:1394`, `:1407`, `:1430`), registered `mutating=True`, dry-run default; backing `github_issues.create_issue:230` / `edit_issue:268` (was read-only at filing) plus new `defect_beads.py`, `issue_standardize.py`. Idempotence honoured. Corroborated against the live MCP roster | `mc-lhd66`, `mc-zib9r` open — **post-landing defects in the shipped surface**, not this issue | — |
| 187 | MBRF034 accepts a fabricated source | ACTIONABLE | `effects.py:418` `if not request.sources:` is verbatim still the entire check — tuple non-emptiness, zero store reads. No MBRF03x code resolves a source id. Precedent for the fix exists one tool over: `work_dispatch_event` "refuses an unresolvable source bead as a precondition" | **no bead** | S |

### Batch D — #188–#206

| # | Title (short) | Disposition | Evidence | Bead(s) | Size |
|---|---|---|---|---|---|
| 188 | `briefs_create` dry_run creates directories | **ALREADY-FIXED** | `678413e` "a dry run must not create directories". `effects.py:563-577` `_require_brief_root` now carries an explicit **"NO mkdir HERE"** block and returns permission only; the mkdir moved to `_atomic_write` on the apply path. **Caveat: no regression test pins the property** | **no bead** | — |
| 189 | 3 formulas deadlock finalize, 76,961 retries | **STALE-PREMISE (residual live)** | Retry storm stopped: `cannot close blocked issue` by day — 08-22: 5438, 08-23: 5064, 08-24: 158, 08-25: 3141, **08-26/27/28: 0**. Both named beads closed 08-24. Budget now bounded upstream (`control-retry-budget … expired=true`). Defect A is not in mathcity source (the edge is minted by gc). **Residual:** the self-blocking graph shape still reproduces — 3 roots minted 08-28T22:10 — but whether they still deadlock is **not established** | `mc-g3i8` open, `mc-i4fg` closed | — |
| 191 | `agent-send.sh` unresolved recipient | ACTIONABLE | `agent-send.sh:71` — `[[ -z "$name" ]] && name="${uuid:0:8}"`, silent; line 104's success line prints only the path. Unchanged since `7715c4f`. Scope: warn on map miss + the prefix-collision warning from the thread | **no bead** | S |
| 192 | `briefs_create` leaves sourceless bead | **ALREADY-FIXED** | `7c5456b` "make a sourced bead create+link one recoverable unit (#192)", merged `83962df`. `beads.py:590` brackets create+link with `COMMISSION_INCOMPLETE_KEY`; `_find_incomplete_partial` adopts a stranded partial on retry instead of minting a twin — removing the accumulating-bricks damage. **Residual:** `effects.py:1420` still says the unconditional "nothing was written" | **no bead** | — |
| 194 | `decisions_to_briefs` deposit undecided | **ALREADY-FIXED** | All four cited commits are ancestors of `51ddb27`. `_handle_decisions_to_briefs` contains no `plan_adjudication`; docstring reads "deposited UNDECIDED … **Do not restore it**"; recurrence guard `test_toolspec_contract.py`. Filed retrospectively | **`mc-yzj` open carrying the PRE-FIX title** ("auto-approves unconditionally") | — |
| 195 | B2.11 check flags MENTIONS | ACTIONABLE (narrowed) | `regions()` still returns the **full file text** for `.py`/`.sh`/`SKILL.md`, so docstring mentions match; the FAIL message names only the register. Two mitigating facts the issue lacks: `role="reader"` already exists (7 entries), and the register header records *why* text-matching was kept (two write-detectors were discarded for missing real writers). So **direction 2 (fix the message) is the actionable part**; direction 1 is arguably already ruled out by a recorded decision | **no bead** | S |
| 196 | `liveness.py` fails city closed on one socket | ACTIONABLE | **Zero** matches for `retry\|attempt\|backoff` across 207 lines (probe validated: 1 match for `create_connection`). Single `socket.create_connection`, single `except OSError → active=False`, consumed as the shared fail-closed gate | **no bead** | S |
| 197 | No typed surface for the worker pool | ACTIONABLE | All four named symbols: **zero** matches under `assets/scripts/`. `EXPECTED_TOOLS` lists 47, none pool-related. `fleet.py` has zero `pool`/`seats` matches (probe validated by 1 `fleet_sessions` hit in the same file) | none exact | M |
| 199 | Six hand-maintained rosters per MCP tool | ACTIONABLE | All six still independent: `mcp_server.py:1757 TOOLS`; `test_mcp_server.py:34 DECLARED_TOOLS`; `mctl_mcp_harness.py:49 EXPECTED_TOOLS`; `client.py:54 ALLOWED_TOOLS` (its own docstring: "spelled out rather than derived from the server's registry"); `fixtures/mcp_tool_schemas.json`; `test_mcp_schema_snapshots.py:116`. **Zero** `REGISTERED_TOOLS` repo-wide; no roster-agreement test | `mc-f63t`/`mc-yga7` are **#162**, not this | S–M |
| 200 | Suite-green vs committed tree | ACTIONABLE | Zero matches for `write-tree\|diff --quiet\|dirty tree\|tested tree` across `scripts/`, `assets/scripts/`, `skills/`, `.github/` (probe validated: 3 `pytest` hits). **`.github/` contains only ISSUE_TEMPLATE + LABELS.md — there is no CI at all** | **no bead** | S |
| 201 | Three review-process rules | **DECISION** | None codified: no `code_hash`/`ast.dump` anywhere; `--append-notes` only in one skill; no review-process rule in any of the 9 `POLICY.md` files. The issue itself states "None requires a code change". Rule 1 is a *relaxation of the review gate* — a human ruling, not engineering | **no bead** | — |
| 202 | mctl emits no `brief.*` event | **ALREADY-FIXED** | New module `mctl_core/gc_events.py` (`89c8e31`). Wired as `on_apply` hooks: `_emit_brief_submitted` (used at `:1380`), `_emit_brief_decided` (at `:1088`). Two tests. **Doorbells observed ringing live** 2026-08-28T22:25 (`brief.submitted`, `brief.decided`). The 10s emit-budget follow-on is #217, a different claim | `mc-d02` open (verbatim mirror, now stale) / `mc-0nf` closed | — |
| 203 | `orders_status` schema violation | **ALREADY-FIXED** | `e20f885` "#203: orders_status emits typed diagnostic objects, not strings". `orders.py` now builds `Diagnostic(...).to_dict()` at the exact string sites named (117/133/146, 189/201). Regression test `test_orders_status_schema.py`. **Caveat: source + test only; the live tool call was not re-run** | `mc-pkgi` open (stale) | — |
| 204 | P0 single-flight gate latches on wisp roots | **STALE-PREMISE** | "Every rig-scoped order has fired at most once, ever" no longer holds: **26 rig-scoped `order.fired` on 2026-08-28 alone**, incl. `brief-shuffle-fast-drain:rig:mathcity` **10×**. `bd list --label "order-run:…:rig:mathcity" --status open` → **`[]`**, no latching root. Remediation executed and closed. Successor is **#218**, not a duplicate | `mc-8ncv` open (stale); `mc-f86q`, `mc-6px0` closed | — |
| 206 | `create_issue_bead` discards `priority/pN` | **ALREADY-FIXED** | `beads.py:31-34` `_PRIORITY_LABEL = re.compile(r"^priority/p([0-4])$")` + `priority_from_labels()`, consumed at `effects.py:754` and `defect_beads.py:95`. Commit `0121fd4` — the **extraction** the filer's own correction named as the only remaining half. Tests at `:267` (p1→1) and `:282` (p3→3) | **no bead** | — |

### Batch E — #208–#225

| # | Title (short) | Disposition | Evidence | Bead(s) | Size |
|---|---|---|---|---|---|
| 208 | recommendation + decision_options at creation | **ALREADY-FIXED** | All three parts on main. `00f707b`: both properties on `briefs_create` **and** `decisions_to_briefs` ToolSpecs, with `_decision_options_section()` refusing a recommendation naming an absent option. `74e38d1`: `no_brainer` + `no_brainer_reason` on `briefs_relay_adjudication`. `fc1c591`: full-form composer. Page layer also met — the verdict×disposition product is replaced by a single `_moves()` control | `mc-iu96` **open** (inverse drift) | — |
| 209 | revise verdicts never come back around | ACTIONABLE | Machinery landed (`revise-return` formula + order, `d21d3ce`); `brief.decided` count now **1141** (issue measured 0 all-time) and the order fired today. Third break unproven: **no `revise-returned.jsonl` exists anywhere** despite **123** `decision:"revise"` events — the re-depositor has never recorded a success. Adjacent: recent payloads carry placeholder `"brief_slug":"mc-open"` | **no bead** | S |
| 210 | freshly-landed MCP tools unreachable | **DECISION** | Two layers with different owners, and the issue's own 08-28 comment overturns half its premise (the dashboard refreshes live via per-request child processes). The remaining half is the **client-side** roster, fixed at session start — outside this repo. It names two mutually-exclusive designs, and the only supported rebind verb is itself #231 | `mc-ggig` open | — |
| 211 | create_github_issue requires UNION of templates | **ALREADY-FIXED** | `98bdfaa` "matches per-template, not the union of every form", merged `4d12140`. `required_template_sections()` now returns `dict[str, tuple[str,...]]` keyed **per template**; `plan_create_github_issue` refuses only when the body satisfies **no** template; the error now names the closest one and says "you need to satisfy only ONE" | **no bead** | — |
| 212 | work_dispatch claim-observer FATAL on success | **ALREADY-FIXED** | `162561d`. `work.py:553-620`: exit-0 sling writes provenance first, returns `applied:true` with `"claim": "observed"\|"pending"`, and MWRK003 is downgraded to **WARN**. `_claim_observed()` accepts assignee OR `execution.work_associated` OR an open child molecule | Sibling defect *from the fix*: `mc-fmxa` open — now **over**-reports `claim:observed` | — |
| 213 | work_dispatch retry mints a duplicate convoy | **ALREADY-FIXED** | Same commit. `_open_child_workflow()` matches a bead with truthy `gc.synthetic` whose `source_dependencies` contain the source id. Landed semantics **refuse a re-sling** rather than adopt — prevents the duplicate, though not literally candidate 1 | Cleanup undone: `mc-u93ib`+`mc-vurp0`, `mc-w1ei`+`mc-jqkc` still duplicate open convoys | — |
| 214 | work_dispatch failure path discards provenance | ACTIONABLE | Still true, narrowed. `append_applied` appears **exactly once** (`work.py:592`, exit-0 path); `append_planned` is imported and **never called**. Both remaining FATAL raises return without flushing (`:528` timeout, `:541`), and `:541`'s text still asserts "no dispatch was recorded" — false after a timeout, when the sling ran and may have minted a molecule. The #212 fix removed the *modal* trigger, not the defect | **no bead** | S |
| 215 | MOPT001 blocks REVISE on multi-option briefs | **ALREADY-FIXED** | `c6f45d9` "scope MOPT001/MOPT002 to approving verdicts only". `effects.py:998-1002` `if offered and normalized == "approve":` with the rationale inline ("gating it would forge a selection the adjudicator never made"). Regression test `test_brief_body.py:388` | **no bead** | — |
| 217 | emit-budget sized honestly | ACTIONABLE | `gc_events.py:47` still `EMIT_TIMEOUT_SECONDS = 10` — a round number citing no measurement, unchanged since `89c8e31`. Contrast the discipline it is asked to mirror: `MEASURED_CATALOG_WORST_SECONDS = 89 + 56`, `MEASURED_SLING_WORST_SECONDS = 162.7`, both with "raise only when a LARGER cost is MEASURED" comments | **no bead** | S |
| 218 | rig-scoped orders never fire after #204 | **STALE-PREMISE — re-scope, do NOT close** | `order.fired` with a `:rig:` subject by day — 08-24: 10, 08-25: 25, 08-26: 20, 08-27: 5, **08-28: 26** — including the exact subject the issue says never fires. **The wall fell; a different one is behind it:** bead `gt-d16kg3` — fast-drain wisp dispatched with **no rig scope**, brief-root unresolvable, step cannot run | none matching | re-scope |
| 219 | required-sections.toml grows §1–§7 | ACTIONABLE | Gate has **not** landed: the file contains exactly **one** `[[section]]` — "Gate Evidence". Its own header still says "brief-check.sh is NOT yet pointed at this file … that is a follow-up". Both enforcement points unwired | `mc-oswv` open / `mc-ryj2` closed (decision) | M |
| 220 | producer full-form passes; retire --compact | ACTIONABLE | Neither half done. Three producer formulas have **0** `§1 —`/`§7` references (control: `brief-prep/SKILL.md` has 7). Compact surfaces all still live: `present-it` 7 hits, `catch-no-brainer` 26 (+9 fixtures), plus 4 formulas. Only the typed composer complies | `mc-hr80` open / `mc-zhol` closed | M |
| 221 | backend-match audit of the verdict panel | ACTIONABLE | No verdict-panel audit exists; the only EXISTS/PARTIAL/ABSENT tables cover the **city** and **triage** dashboards, which the issue names as different scope. **Re-scope warning:** ADR 0004's control matrix is itself stale — the panel was rebuilt into a single `_moves()` control, so "verdict radios + disposition input" no longer exist as separate controls. Ground the audit on current `panel.py`, not the ADR | `mc-0ppp` open / `mc-x6dz` closed | M |
| 224 | closeability triage of every open issue | ACTIONABLE (META, in flight) | Asks for exactly this sweep; this report is 89 of its ~107 rows. Its seed list is partly confirmed by independent probe (#208, #211, #215 indeed fixed; #218 refuted — the shape it predicted). Note its explicit constraint: **no issue closed by the auditor**; the close batch is separately human-gated | `mc-mivv` open / `mc-66dw` closed | M |
| 225 | probe every NOT-PROBED MCP tool | ACTIONABLE (mostly done) | **16 of 17** named tools now carry a cited verdict + trace id: 15 WORKS, `briefs_options` DEGRADED, `commission_brief` **BROKEN (dry-run crashes)**. Residual: `trace_show` still the one NOT PROBED row; the `commission_brief` bug-report brief is the actual deliverable; loop-table rows still contradict §2. **Caveat: `docs/SURFACE-STATUS.md` is gitignored and absent from `origin/main`** — the evidence is a local untracked file, not repo state | `mc-jvqq` open | S |

### Batch F — #226–#245

| # | Title (short) | Disposition | Evidence | Bead(s) | Size |
|---|---|---|---|---|---|
| 226 | typed rejection/pile-state reader | ACTIONABLE | Enumerated all **48** registered tools — none reads pile/rejection state; `gates_status` serves definitions only. The artifact still exists and is still written: `brief-shuffle-fast-drain.py:669` writes `.pile/.rejected/<slug>/rejection.json` | `mc-55de2` open, `mc-yu0u9` closed. **No bead mirrors this issue** | M |
| 227 | no mathcity control-dispatcher / brief-operators | **STALE-PREMISE** | 2026-08-28 `tmux -L gt ls`: `mathcity--core__control-dispatcher-pool` present, plus `mathcity__brief-operator-{1,2,3}-pool`. The stated symptom does not hold. Root cause of the 08-24 non-respawn was never identified and lives in the gascity supervisor, not in this repo — **could recur** | `mc-ko8d0` **in_progress** (root-cause), `mc-nhiaq`, `mc-idmly` open | — |
| 228 | work_dispatch re-slings same source | **ALREADY-FIXED** | `162561d` (merge `670f7ac`), dated after the issue. `_select_dispatch_source` docstring: "skipping any already being worked (#228)"; `_source_dispatchable` refuses on `has_active_assignee`, `_open_child_workflow`, `_has_dispatch_provenance`; `MWRK002` blocker at `:927`. This is the issue's own preferred candidate 1 | adjacent only | — |
| 229 | briefs_list drops body/sections/options | ACTIONABLE | `list_briefs_report` passes `bodies` **only** to `_document_records`; the bead lane `_records(...)` never receives it. `BriefRecord.to_dict` emits the four fields only `if self.body is not None`. `body_elided` is set at exactly one site, so a bead-sourced record keeps the `None` default. Claim holds verbatim | `mc-x8a1` open (mirror), `mc-26lk` open | S–M |
| 230 | commission_brief NameError | **ALREADY-FIXED** | `effects.py` **line 51**: `from .commission import brief_labels, tracker_metadata, validate_commission`. Landed in `fd214f2`, ~5h after the issue was filed | **Five beads, one defect:** `mc-99jj` open + `mc-evc2` open (two mirrors) + `mc-ne2w`, `mc-i07a`, `mc-j6uh` closed | — |
| 231 | verify dashboard rebind end-to-end | ACTIONABLE (small residual) | Part 2 settled: both `dashboard_status` and `dashboard_restart` call the same `serving.read_commit(serving.PACK_ROOT)` — single source of truth. Part 1 mostly covered by `test_dashboard_rebind_endtoend.py` (`d993721`) — real port, asserts new pid ≠ old, serves 200. Residual: no assertion that `serving_commit` advances, and no live `dry_run:false` apply | `mc-6i9gm` open | S |
| 232 | evidence core: artifact:recorded / expected_artifacts | **DUPLICATE of #233** | Bodies are **byte-identical**: same md5, `diff` exit 0, 15,179 bytes each. #232's *body* is entirely the B3.1 defect. **But its TITLE names a different, real, unfiled defect** — `molecules.py:59 _ARTIFACT_PREFIX = "gc.build."`, `:270`/`:279` derive `artifact: recorded` from metadata, never from disk. That content was lost at filing time — see the cross-cutting note | none for either | — |
| 233 | bead closes on a verdict with no acceptance check | ACTIONABLE | `git grep -ln "acceptance_criteria" origin/main -- 'assets/*'` → **rc=1, no output**; `acceptance` in `mctl_core/` → empty. **Control probe:** `plan_adjudication` in the same file returns 1 hit, so the probe can return non-empty. `plan_adjudication` at `effects.py:928`; unconditional `status="closed"` at `:1103`; `materialize_plan.py:415`. Every claim verified | **no bead anywhere in the 1,166-row store** | L |
| 235 | §4 options invalidated by later measurement | ACTIONABLE | `deposited_at` in `briefs.py` → empty; no staleness diagnostic (MBRF040/041/042 are body-*presence* WARNs, not freshness). `decision_options` still resolves from the bead description then the markdown cache — notes are not a source. `brief-check.sh:310` still a literal-token grep. All four root-cause sites unchanged | **no bead** | M |
| 236 | fast-drain reads pile membership from directory | **ALREADY-FIXED** | `57dbe38` "read pile membership from the bead store, not the directory … (#236)". `selected_pile_items` now returns `[row.path for row in memberships if row.is_member][:max_items]` — no `iterdir`; `remaining_pile` recomputed over members. Regression test +104 lines | 4 open beads are *different* fast-drain defects | — |
| 237 | rollup: no close path + assignee guard exit-1 | **ALREADY-FIXED** | Same commit `57dbe38`. `group_repair_status()` at `:57`, "omit any group whose repair bead is closed" at `:75`, open/closed/unknown tally at `:78`, and the guard at `:172-175` now prints "skipping this group" and `continue`s instead of `exit 1`. Smoke test +23 lines | **no bead** | — |
| 238 | bead reaping BP4.2(c)/BP4.5 zero enforcement | ACTIONABLE (+ decision half) | `git grep -n "BP4\." origin/main -- assets/ skills/ subdomains/` → **empty**. Control: the same grep tree-wide returns exactly 3 docs (`POLICY-beads.md`, `README-beads.md`, `rule-prefix-registry.md`) — zero call sites. **Decision component:** the 16 over-closed `hecke` beads (incl. the circular collapse with no open survivor) are data repair needing a human ruling; only BP4 enforcement is straight engineering | **No bead mirrors this issue** | L |
| 244 | dashboard MCP calls serialize on one lock | ACTIONABLE | `client.py:159` still `self._lock = threading.RLock()`, taken in `_request`. **PR #243 pins the defect rather than fixing it** — `test_concurrent_calls_overlap` is `xfail(strict=True)`. Useful: the issue's "root cause not yet identified" is now **identified** — the single shared pipe behind that lock. No client pool landed | `mc-znfnm` open P1, `mc-8ij1` open | M |
| 245 | typed bead READ — beads_list/beads_show | **ALREADY-FIXED** | PR #251, commit `485751e`, merged as `51ddb27` (current tip). New `mctl_core/bead_reads.py`; its `scope` block emits `statuses_excluded`, `matched`, `total_in_store`, `has_verdict_filter`, `limit_applied`, `truncated` — **the "declares the scope it applied" requirement is met**. Registered at `:139`/`:3105`/`:3142`; roster now 48. **Two secondary residuals:** not in `client.py ALLOWED_TOOLS` (control: `briefs_list` *is* there); `created_by → "attributing": false` not implemented | **no bead** | XS residual |

---

## WHAT I COULD NOT DETERMINE

**Zero issues were dispositioned UNKNOWN.** All 89 settled with a probe that could have returned the other answer. That is an unusual result and deserves scrutiny rather than credit, so every bounded-confidence caveat the six agents flagged is listed here rather than buried. **These are the places where a row is narrower than it looks.**

| # | Disposition given | What was NOT established |
|---|---|---|
| 79 | ACTIONABLE | Only the same **6 of 18** databases the issue sampled were re-measured. The other 12 are unchecked, and no check was made of whether a sync path is even configured for them. Calling them healthy *or* unhealthy would be the exact vacuous-check shape the issue is about |
| 61 | ACTIONABLE | Verified only that the *audit artifact* does not exist. **No probe was run live**, so the issue's actual question — do the probes report what they claim — is untouched by this triage |
| 72 | ACTIONABLE | **1 of 18** canaries re-measured (`gs-ab5v`). The other 17 are inferred from that one plus the absence of any back-filter |
| 123 / 124 | ACTIONABLE / DECISION | The current dangling-root count was **not** re-measured; the agent hit a `bd` i/o timeout mid-run. Rows rest on code-side evidence plus the issue's own census. **I re-probed at 18:36 — `gc dolt status` running, `bd list --json` exit 0, 2.5 MB returned. The degradation was transient, not an outage; a fresh count is obtainable** |
| 99 / 105 | STALE-PREMISE / DECISION | Measured on the `<city-root>/gascity` checkout @ `38134e53a` (2026-08-10), ~18 days stale, because `<repos-root>` is out of lane. Direction is safe for both (an older tree containing the defect cannot prove a later fix; an older tree containing `probe_pool_demand` proves it predates the filing). Neither observed a live claim, which both issues name as their own gating unknown |
| 171 | ACTIONABLE | Confirmed live failure from three tracking wisps today; the **158/163 counts were not re-derived** (needs `gc order check`, measured >120s) and the sweep was not run (it mutates) |
| 180 | ACTIONABLE | Verified the plan's account of steps 1–4 and that its gate (#181) has cleared; **did not find a molecule root bead** satisfying the DoD, and did not run the lap (it mutates). "Step 5 not demonstrated" is a measured absence of evidence, not proof it has never completed |
| 189 | STALE-PREMISE | The retry storm is measurably over, but the self-blocking finalize graph **still reproduces** (3 roots minted 08-28T22:10). Whether they still deadlock was not established — they had not reached finalize at measurement time |
| 203 | ALREADY-FIXED | Verified from **source + regression test only**. The mctl MCP server disconnected mid-session, so the live `orders_status` call was not re-run. **If you re-probe exactly one ALREADY-FIXED row, make it this one** |
| 188 | ALREADY-FIXED | The fix is present and explicit, but **no regression test pins the property** — it can silently regress |
| 225 | ACTIONABLE | The ledger it audits (`docs/SURFACE-STATUS.md`) is **gitignored and absent from `origin/main`**, so its state cannot be pinned to a commit the way every other row here can |
| 231 | ACTIONABLE | The remaining half needs a live `dashboard_restart` with `dry_run:false` against an instance served from the `<repos-root>` checkout — outside this lane and unavailable to read-only triage |
| 210 | DECISION | The client-side MCP roster half is **unverifiable from inside a single session** (it would require landing a tool and calling it from a session that predates the landing). That is *why* it is DECISION and not an engineering disposition |
| 227 | STALE-PREMISE | The current fleet contradicts the premise, but **which layer dropped mathcity from the 08-24 respawn set was not determined** — so recurrence cannot be ruled out. Live on `mc-ko8d0` |

---

## Verification of the three claims I was asked to check rather than trust

**1. "22 of 55 sampled issues have a verbatim duplicate bead; #230 has three."**
**Direction confirmed, magnitude corrected upward.** Of the 89 in-scope issues, **39 have a bead whose title matches at ≥0.80** (37 of them exactly 1.0); loosening to ≥0.72 plus explicit issue-number beads gives **43 of 89** — 44–48%, against the claimed 40%. **#230 does not have three beads; it has five** (`mc-99jj` open, `mc-evc2` open, `mc-ne2w`, `mc-i07a`, `mc-j6uh` closed), two of them *open mirrors of each other* for a bug fixed three days ago.

**2. "105 of 112 closures (94%) carry no linked PR or commit."**
**Confirmed.** GraphQL over the 100 most recent closed issues: **96 have no `ClosedEvent` closer at all**; only 4 carry a linked PR or commit. That is 96%, marginally worse than claimed. **The closed half of this tracker is not usable as a cross-check** — which is why every row above was probed against source instead.

**3. "Bead-hygiene drift: an earlier closed bead carrying the full body plus an open mirror; #55's two beads are BOTH closed."**
**This needs a correction, and it is the most consequential finding in the report.**

The shape is real and common — it appears on **35 of 89** issues — but **it is overwhelmingly NOT drift.** I opened the closed beads. They are `type: decision` **adjudication beads** whose verdict was *"commission this work."* Closing them is *correct*: the decision was made. The open bead is the **work**, which has not happened. All 37 `github-issue N — …` beads in the store are closed, and the eight `Issue-drain batch N` decision beads (`mc-28w3`, `mc-qczc`, `mc-e6j3`, `mc-6u9u`, `mc-mdoi`, `mc-i1xf`, `mc-mxuf`, `mc-4zo5`) are likewise closed — yet `bd dep tree` shows **every one of their children still open.**

> **Operational consequence: anyone reading closed-bead status as "fixed" will be wrong on every one of these.** Four of the six agents hit this shape independently and reached the same conclusion.

The **genuine** drift runs the opposite way, and it is widespread:

- **Open beads carrying pre-fix titles** for issues now fixed — `mc-d02` (#202), `mc-pkgi` (#203), `mc-8ncv` (#204), `mc-iu96` (#208), `mc-ot5k` (#94), and `mc-yzj`, which still says #194 "auto-approves unconditionally" long after the fix merged.
- **Issues with no live bead at all** — at least **30 of 89**, including **#233, #235, #238** (three of the largest still-true defects in the tracker), plus #65, #67, #88, #95, #96, #115, #125, #140, #163, #168, #171, #183, #187, #191, #196, #197, #199, #200, #201, #214, #217.
- **The `#99` number-collision trap is live**: `mc-diuc` and `mc-i4fg` both contain the string `#99` but refer to a *gascity PR*, not issue 99. This is the exact false match that already caused #99 to be wrongly closed and reopened once. **Any automated bead-matching sweep over this tracker will hit it again.**

*(The #55 shape — all matching beads closed while the issue is open — did **not** recur anywhere in the 89. The nearest case, #99, is the number-collision above, not a genuine all-closed pair.)*

---

## Cross-cutting findings

**The false-open rate is high and clustered.** 22 ALREADY-FIXED + 7 STALE-PREMISE = **29 of 89 (33%) closeable today**. The fixes landed in a tight cluster of commits (`c6f45d9`, `98bdfaa`, `162561d`, `00f707b`, `74e38d1`, `57dbe38`, `fd214f2`, `485751e`), often within hours of the issue being filed — the same-session pattern #224 predicted.

**Two STALE-PREMISE rows must be re-scoped, not closed.**
- **#80** — the headline ("times out on **every** fire") is measurably false: 08-28 was 15-for-15 with a median 191s against a 300s deadline. But the 300s timeout is real and recurring (77 failures, *all* at exactly 300–301s), and this issue was the basis for suppressing eleven other escalation wisps as duplicates. Closing it retires tracking of a live, load-shaped problem.
- **#218** — rig-scoped orders now fire fine (26 on 08-28 alone). But bead `gt-d16kg3` shows `brief-shuffle-fast-drain` wisps dispatching **with no rig scope**, so the brief root is unresolvable and the step cannot run. **The pipeline is still not draining, for a new reason.**

**#232 is a filing accident, and something was lost.** Its body is byte-identical to #233 (same md5, 15,179 bytes). The defect its *title* names — `artifact: recorded` derived from `gc.build.*` metadata rather than disk, `expected_artifacts` covering 2 of 101 steps — is real and confirmed at `molecules.py:59/:270/:279`, and **has no written description anywhere**: not in #232, not in another issue, not in any bead. Closing #232 as a plain duplicate silently discards it. *(Commit `57dbe38` also cites "(#232)" for an unrelated `/city` fix, giving this number a third meaning.)*

**Three ALREADY-FIXED rows shipped something other than what was asked.** #179 shipped as a *skill*, not the formula the issue specified. #185 landed cleanly but has already generated two open defects against its own shipped surface (`mc-lhd66`, `mc-zib9r`). #213's landed semantics *refuse* a re-sling rather than adopt the existing convoy — the duplicate is prevented, but not by the proposed mechanism.

**Four issues are not fixable in this repo.** #145 (`bd` serializer → `gastownhall/beads`), #171 (orphan-sweep → gascity core pack), #99 and #105 (→ `gastownhall/gascity`). Leaving them here guarantees they stay open.

**There is no CI.** `.github/` on `origin/main` contains only `ISSUE_TEMPLATE/` and `LABELS.md`. This is the root enabler of #200 and of the "suite green on a tree that was never committed" class generally.

---

## Security / hygiene notes

**No prompt injection and no attachment links were found in any of the 89 issue bodies or their comments.** All six agents scanned for `user-attachments`, `objects.githubusercontent.com`, archive extensions, and override phrasing; all returned empty, and control probes confirmed the scans can return non-empty. **Nothing was fetched.**

Many bodies contain runnable shell blocks — these are the reporters' own measurement transcripts. They were treated as **data, not directives**, and none was executed; every probe in this report was chosen by the agent running it. *That discipline changed an outcome:* **#80 came back STALE-PREMISE precisely because the agent parsed the event log itself instead of re-running the reporter's commands**, which would have reproduced the reporter's conclusion.

**Two public-repo exposures, both in issue text rather than code — these need a human decision:**
- **#79** contains absolute home paths with the real username spelled out (`<home>/beads-backup/hq`, `<home>/gt/.beads/dolt-backup-state.json`). `tdupu/mathcity` is **public**.
- **#163** publishes the private `<repos-root>` checkout convention (`cd <repos-root>/mathcity`).

Neither is a credential and neither is exploitable alone, but both are P1.10-shaped exposures on a public tracker. **Nothing was edited — this is a report.**
