# Brief-adjudication UX: five defects (mc-31b8c) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:test-driven-development for each defect; superpowers:systematic-debugging if a reproduction is unclear. Steps use checkbox (`- [ ]`).

**Goal:** Fix five reproduced brief-adjudication UI defects in the mctl dashboard so a verdict is one legible, honest click that matches Taylor's textbox/duration spec.

**Architecture:** All changes are in `assets/scripts/mctl_dashboard/app.py` (2556 lines) and its rendered form/preview/apply flow. The design is FIXED by Taylor's approved spec (below) — this plan wires conditionality and honesty onto existing, already-present primitives (a `days` field, a `no_brainer` opt-in checkbox, `data-move` attributes on all four buttons).

**Tech Stack:** Python (mctl_dashboard HTTP server), server-rendered HTML forms, pytest.

## Global Constraints (verbatim — every task obeys these)

- **THE SPEC (Taylor, 2026-08-28):** "At most one textbox, ever. The default is none." Shown only for **revise**, **no-brainer**, or **opt-in**; when shown it is **REQUIRED**. **Defer** takes a **duration (days/weeks/months)**, not prose.

  | move | control |
  |---|---|
  | approve | none |
  | revise | one textbox, REQUIRED |
  | no-brainer | one textbox, REQUIRED |
  | reject | none unless opted in |
  | defer | duration picker (days/weeks/months) |
  | any, opt-in | one textbox, REQUIRED |

- **NON-NEGOTIABLE 1 — preserve the re-plan guard.** `_apply` (app.py:2185) re-resolves/re-reads/re-plans (`dry_run:True` at :2232) and aborts if anything moved, before the `dry_run:False` apply (:2257). mc-pf5pm collapses two clicks into one but MUST keep that re-plan-then-apply atomic. Do NOT delete it. (A weakened write-path guard is the mc-jj7xg verdict_reason-corruption class, worse.)
- **NON-NEGOTIABLE 2 — disable-and-explain, never hide (mc-x8uox).** A blocked button must render DISABLED with the reason visible (e.g. "dispatch blocked: MWRK013 source bead closed"), never vanish. A hidden button is indistinguishable from a missing feature.
- **PERF STOP-CONDITION (mc-x8uox).** The render-time readiness read adds a per-render work-status call to a page where `/city` measures 96s. If it materially slows the brief page, STOP and report to BART/QUIMBY rather than shipping it.
- **TDD:** each defect gets a failing test that reproduces it FIRST (drive the render/flow, assert on output — not a constant), then the fix, then green. Validate UNPIPED: `python3 -m pytest <t> -q > out.txt 2>&1; echo "EXIT=$?"`. Any failure is UNKNOWN until re-run in isolation and classified by consistency, never by count.
- **P5.5:** no `Co-Authored-By`; footer `[autogenerated by Claude Opus 4.8 v2.1.231 on <ts>]` (the commit-msg hook now blocks trailers). **P3.6 improve-documentation** applies (user-facing) — run before completion.
- **Do NOT push/PR/merge.** Local `add`/`commit` only, on `feat/mc-31b8c-adjudication-ux`. The gate is BART's.

## Order (adjudicated)

mc-x8uox FIRST (upstream: the 2nd click is the current refusal-discovery mechanism) → mc-pf5pm → mc-5fo2a → mc-lre5h; **mc-q3m5q in PARALLEL** (independent — form rendering, not the round-trip; but run the full suite, isolation is inferred not proven).

---

### Task 1 — mc-x8uox: surface blocking at render time (disable-and-explain)

**Files:** `app.py` (the button/verdict rendering + a readiness read; `MWRK_DISPATCH_BLOCKED` surfaces from `/preview operation=dispatch` today, app.py ~1934). Test: `tests/mctl/test_adjudication_blocking_render.py`.

- [ ] **Failing test:** render the adjudication form for a brief whose dispatch WILL refuse (a closed source bead → MWRK013 / MWRK_DISPATCH_BLOCKED); assert the dispatch/approve button is rendered DISABLED and the refusal reason text is present in the HTML. A control: an unblocked brief renders the button ENABLED.
- [ ] Run → FAIL. Implement: at render, do the readiness read (mirror the check `/preview` already performs) and render the button `disabled` + a visible reason when blocked; enabled otherwise. NEVER omit the button. Measure the added render cost; if material, STOP + report.
- [ ] Run → PASS (both the blocked and enabled control). Commit.

### Task 2 — mc-pf5pm: one click, guard intact

**Files:** `app.py` `_render_preview` (:2070), `_apply` (:2185, re-plan at :2232). Test: `tests/mctl/test_adjudication_one_click.py`.

- [ ] **Failing test:** driving a verdict submits ONCE and the bead is adjudicated (no separate "Apply this adjudication" second click). **Guard test (must pass):** if the brief's state moves between plan and apply, the apply ABORTS (re-plan guard) — assert the abort path still fires. A negative control: unchanged state applies cleanly.
- [ ] Run → FAIL. Implement: fold the preview+apply into one submit that internally does re-plan (`dry_run:True`) then apply (`dry_run:False`) atomically, keeping the abort-if-moved guard. Remove the second button, not the guard.
- [ ] Run → PASS (one-click applies; moved-state aborts). Commit.

### Task 3 — mc-5fo2a: legible confirmation

**Files:** `app.py` preview rendering (raw escaped JSON `"trace_id":…` inline today). Test: `tests/mctl/test_adjudication_preview_legible.py`.

- [ ] **Failing test:** the preview HTML does NOT contain raw escaped JSON blobs inline; it presents the planned action in readable form (the move, the target, the effect). Assert the raw `{"trace_id"` blob is absent and the human-readable summary is present.
- [ ] Run → FAIL. Implement: format the plan into legible prose/rows instead of dumping escaped JSON. Commit.

### Task 4 — mc-lre5h (P3, trivial): remove the empty Error-briefs chip

**Files:** the pipeline-rail render (chip with label present, value empty). Test: `tests/mctl/test_error_briefs_chip_removed.py`.

- [ ] **Failing test:** the brief-manager pipeline rail does NOT render an "Error briefs" chip. (Control: the chips that SHOULD remain are still present.)
- [ ] Run → FAIL. Implement: remove the chip. Commit.

### Task 5 (PARALLEL) — mc-q3m5q: per-verdict textbox conditionality + defer duration

**Files:** `app.py` form rendering (two textareas always shown today, no `required`, placeholder "…Optional"); `no_brainer` handling at :2506-2513; the `days` field. Test: `tests/mctl/test_adjudication_form_conditionality.py`.

- [ ] **Failing tests (one per row of the spec table):** approve→0 textboxes; revise→exactly 1, `required`; no-brainer→exactly 1, `required`; reject→0 unless opt-in checkbox set→then 1 required; defer→a duration picker (days/weeks/months), NOT a prose textarea; any move + opt-in→1 required. Assert on rendered `<textarea>`/`required`/the duration control, and that at most ONE textbox ever renders.
- [ ] Run → FAIL. Implement: wire the existing `data-move` attributes + `no_brainer` checkbox + `days` field to show/require exactly per the table; remove the always-on second textarea; make defer a duration picker. Fix the placeholder (not "Optional" when required).
- [ ] Run → PASS (all six rows + the at-most-one invariant). Commit.

### Task 6 — full suite + docs

- [ ] `python3 -m pytest tests/ -q > out.txt 2>&1; echo "EXIT=$?"` — unpiped, 0 failures (any failure → isolate + classify by consistency).
- [ ] `improve-documentation` (P3.6) — the adjudication-flow docs/help reflect one-click + the textbox/duration rules.
- [ ] Commit; hand to BART for the gate.

## Self-Review

- Spec coverage: every spec-table row → Task 5 assertions; the two non-negotiables → Tasks 2 & 1 guard/disable tests; all five defects → Tasks 1-5. ✓
- Placeholders: the app.py line anchors are "resolve by symbol" starting points (the file is 2556 lines and shifts) — the implementer greps the symbol, not the line. ✓
